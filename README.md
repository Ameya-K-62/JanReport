
  # Local News Reporting Platform

  This is a code bundle for Local News Reporting Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
